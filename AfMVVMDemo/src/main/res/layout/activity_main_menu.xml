<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:binding="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="cn.appoa.afmvvm.viewmodel.MainMenuViewModel" />

        <variable
            name="adapter"
            type="cn.appoa.afmvvm.adapter.MainMenuAdapter" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/title_bar"
            layout="@layout/title_bar"
            binding:titleBar="@{viewModel.getTitleBarViewModel()}" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:overScrollMode="never"
            binding:adapter="@{adapter}"
            binding:decorType="@{1}"
            binding:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            binding:supportsChangeAnimations="@{false}" />

    </LinearLayout>

</layout>